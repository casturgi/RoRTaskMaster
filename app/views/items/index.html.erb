<% if user_signed_in? %>
	<%= link_to "Create New Item", new_item_path %>
	<% @items.each do |item| %>
		<h2><%= link_to item.title, item %></h2>
		<p><%= item.description %></p>
	<% end %>
<% else %>
	<%= render 'welcome_page' %>
<% end %>

