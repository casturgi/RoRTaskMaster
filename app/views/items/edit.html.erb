<div class="col-md-8 col-md-offset-2">
	<h1>Edit To-Do Item</h1>

<%= simple_form_for ([@task, @item]) do |f| %>
	<%= f.input :title, label: "Item Title" %>
	<%= f.input :description %>
	<%= f.button :submit %>
<% end %>
</div> 