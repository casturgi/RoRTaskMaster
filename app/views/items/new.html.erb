<div class="col-md-8 col-md-offset-2">
	<h1>New To-Do Item</h1>

	<%= simple_form_for ([@task, @task.items.build]) do |f| %>
		<%= f.input :title, label: "Item Title" %>
		<%= f.input :description %>
		<%= f.button :submit %>
	<% end %>

</div> 