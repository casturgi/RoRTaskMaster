<div>
	<h1><%= @task.name %></h1>
	<p><%= @task.description %></p>
	<p><%= link_to "Delete", task_path(@task), method: :delete, data: { confirm: "Are you sure you want to delete this project"} %></p>
</div>

<div>
	<%= link_to "Create New Item", new_task_item_path(@task) %>
	<%= render @task.items %>
</div>


